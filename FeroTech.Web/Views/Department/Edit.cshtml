@model FeroTech.Infrastructure.Application.DTOs.DepartmentDto

@{
    ViewData["Title"] = "Edit Department";
}

<h2 class="mb-3" style="color: white; text-align: center;font-size:50px">Edit Department</h2>
<hr />

<form id="editDepartmentForm">
    <input type="hidden" asp-for="DepartmentId" />

    <div class="row">
        <!-- Department Name -->
        <div class="col-md-6 mb-3">
            <label class="form-label" style= "color: white; text-align: center;">Department Name</label>
            <input asp-for="DepartmentName" class="form-control" />
            <span asp-validation-for="DepartmentName" class="text-danger"></span>
        </div>

        <!-- Description -->
        <div class="col-md-6 mb-3" style= "color: white; text-align: center;">
            <label class="form-label">Description</label>
            <input asp-for="Description" class="form-control" />
        </div>

        <!-- Active -->
        <div class="col-md-6 mb-3 d-flex align-items-center" style="color: white; text-align: center;">
            <label class="form-label me-2">Active:</label>
            <input asp-for="IsActive" type="checkbox" class="form-check-input" />
        </div>

        <div class="col-md-12 text-end">
            <button type="submit" class="btn btn-success">
                <i class="fa fa-save"></i> Update
            </button>
            <a asp-action="Index" class="btn btn-secondary">
                <i class="fa fa-arrow-left"></i> Cancel
            </a>
        </div>
    </div>
</form>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <script>
        $(document).ready(function () {
            $("#editDepartmentForm").on("submit", function (e) {
                e.preventDefault();

                Swal.fire({
                    title: "Confirm Update",
                    text: "Do you want to save changes?",
                    icon: "question",
                    showCancelButton: true,
                    confirmButtonColor: "#3085d6",
                    cancelButtonColor: "#d33",
                    confirmButtonText: "Yes, Update it!"
                }).then((result) => {
                    if (result.isConfirmed) {
                        $.ajax({
                            url: '@Url.Action("Edit", "Department")',
                            type: 'POST',
                            data: $(this).serialize(),
                            success: function (response) {
                                if (response.success) {
                                    Swal.fire({
                                        title: "Updated!",
                                        text: response.message,
                                        icon: "success"
                                    }).then(() => {
                                        window.location.href = '@Url.Action("Index", "Department")';
                                    });
                                } else {
                                    Swal.fire("Error!", response.message || "Something went wrong.", "error");
                                }
                            },
                            error: function () {
                                Swal.fire("Error!", "Server error occurred.", "error");
                            }
                        });
                    }
                });
            });
        });
    </script>
}
